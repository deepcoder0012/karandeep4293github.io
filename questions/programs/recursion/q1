#include<stdio.h>
int factorial(int n);
void main() {
    int n=5;
    printf("Factorial of %d = %d", n, factorial(n));
}

int factorial(int n) {
    if (n>=1)
        return n*factorial(n-1);
    else
        return 1;
}


    ---OUTPUT---
    Factorial of 5 = 120

    