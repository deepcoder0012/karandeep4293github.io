#include<stdio.h>
int sum(int n);
int s;
void main(){
    int n=10;
    printf("sum : %d",sum(n));
}

int sum(int n){
    if (n==0)
        return s;
    else{
        s=s+n;
        sum(n-1);
    }
}

    ---OUTPUT---
    sum : 55